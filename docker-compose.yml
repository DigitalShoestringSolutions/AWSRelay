services:
    aws_relay:
        build: .
        volumes:
            - ./config:/app/config
        networks:
            default:
        logging:
            driver: syslog
            options:
                tag: docker-aws-relay
        restart: unless-stopped
